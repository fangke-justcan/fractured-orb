# Fractured Orb 噪点成因分析报告

**分析对象**: 本地 Shadertoy 副本《Fractured Orb》(原作 tdhooper, 2021) 的画面颗粒噪点
**分析方法**: 控制变量 + 帧间像素差分测量（暂停动画、冻结旋转、固定静止相位，只改变目标变量，对两张帧求逐像素平均绝对差）
**测量单位**: 0–255 色阶（越大 = 噪点越强）；"空间高频"为单帧内相邻像素差分（含边缘与颗粒）
**测试环境**: 1280×720, DPR=1, WebGL2, Windows

---

## 一、结论（TL;DR）

| 候选原因 | 判定 | 依据 |
|---|---|---|
| **参数问题（采样率）** | ✅ **主因** | 色散采样数从 1→8，噪点差分从 6.25→2.32（空间高频 11.51→2.27，降为 1/5） |
| 性能问题 | ❌ 无关 | 最高画质（色散8×弹射10）实测 60fps 满帧；性能只影响流畅度，此 shader 无时间累积机制，帧率高低不改变噪点多少 |
| 移植/其他问题 | ❌ 排除 | ① 同一时刻渲染两帧逐像素差分 = 0.00（渲染 100% 确定，无不稳定因素）② Shadertoy 原版预览有同样的紫色颗粒 |

**一句话结论**: 噪点是原作者的**有意设计**——用"每像素每帧掷一次骰子的随机波长色散"替代固定波长的色带。它由两个参数决定强弱：**色散采样数**（产生端）和**景深模糊半径**（掩盖端，景深是这个 shader 里事实上的降噪器）。

---

## 二、噪点的视觉特征

- 彩色（紫/青为主）细颗粒，集中在玻璃体的表面棱边、暗部和内部折射路径上
- **背景纯色区域完全无噪点**（所有配置实测差分恒为 0.00）
- 播放时颗粒每秒"重掷"60 次产生闪烁感；闪烁比颗粒本身更吸引注意力

对比截图（`analysis/` 目录）：

| 文件 | 内容 | 噪点观感 |
|---|---|---|
| `00-原版shadertoy-球体.png` | Shadertoy 官方页面 | 球体暗部有明显紫色颗粒（**原版就有**） |
| `04-本地球体-色散5.png` | 本地·球体·默认 | 与原版颗粒特征一致 |
| `01-本地方块-色散5.png` | 本地·方块·默认 | 棱边可见彩色颗粒 |
| `02-本地方块-色散1.png` | 本地·方块·色散=1 | 全屏密集颗粒，噪点最重 |
| `03-本地方块-色散8.png` | 本地·方块·色散=8 | 明显干净 |

---

## 三、产生机制（代码级）

噪点的源头在 `shaders/bufferA.frag` 的色散循环里（mainImage 内）：

```glsl
// 每像素每帧从蓝噪声纹理取 1 个随机数
float rand = texture(iChannel0, (fragCoord + floor(iTime * 60.) * 10.)
                    / iChannelResolution[0].xy).r;
// 该光线"代表"的波长 = 分层基底 + 随机抖动
wavelength = disperse / uMaxDisperse;
wavelength += (rand * 2. - 1.) * (.5 / uMaxDisperse);
...
float ior = mix(uIorMin, uIorMax, wavelength);   // 折射率随波长变化
```

因果链：

1. **每像素只有 1 个随机波长采样**——相邻像素拿到不同波长 → 折射率不同 → 光线在玻璃内部经多次弹射走出截然不同的路径 → 颜色剧烈分歧，形成彩色颗粒。
2. **`floor(iTime * 60.) * 10.`**——每帧取蓝噪声纹理的不同区域，相当于每秒重掷 60 次骰子 → 颗粒每帧刷新，产生"闪烁"。
3. **这是设计而非缺陷**：单帧渲染做光谱色散只有两条路——固定波长（产生色带 banding）或随机波长（产生颗粒 grain）。作者选择了后者，并用蓝噪声让颗粒看起来像胶片颗粒（原作 tags 里就标注了 `bluenoise`）。Shadertoy 平台无帧间累积，这是该平台做色散的标准做法。
4. **Image 通道的景深是唯一的"降噪器"**：bokeh 模糊对每个屏幕像素沿金角螺旋平均约 100 次缓冲采样，无差别地把噪点也平均掉了。

---

## 四、实验数据

### E0 确定性对照（排除移植/驱动不稳定）
同一 `iTime` 连续渲染两帧：

| 区域 | 逐帧差分 |
|---|---|
| 物体区（中心 45% 裁剪） | **0.00** |
| 背景区 | **0.00** |

→ 渲染完全确定。噪点不是 GPU/驱动/移植层的随机性，而是 shader 主动写入的。

### E1 噪点空间分布（默认画质：色散5 / 弹射10 / 景深开）

| 区域 | 逐帧差分 | 空间高频 |
|---|---|---|
| 物体区 | 2.48 | 2.60 |
| 背景区 | **0.00** | **0.00** |

→ 噪点 100% 来自穿过玻璃的色散路径。

### E2 色散采样数扫描（第一主导因素）

| 色散采样数 | 逐帧差分（物体） | 空间高频 | 对比截图 |
|---|---|---|---|
| 1 | 6.25 | **11.51** | `02-*.png` 颗粒密布 |
| 3 | 2.76 | 3.22 | |
| **5（默认）** | 2.48 | 2.60 | `01-*.png` |
| 8 | 2.32 | **2.27** | `03-*.png` 最干净 |

→ 1→8 空间噪点降为 **1/5**；低采样端改善尤其剧烈。这是面板里性价比最高的降噪旋钮。

### E3 弹射次数的影响
弹射=1（色散5，其余默认）：逐帧差分 **0.00**，空间高频 0.00。

→ 弹射 1 次时射线终止于第一个表面，随机波长的折射路径从未被追踪，**噪声完全消失**（同时画面失去所有玻璃内部结构）。这从反面证明：噪点 = 多次弹射 × 随机波长的路径分歧。

### E4 景深 = 事实上的降噪器（第二主导因素）

| 景深设置 | 逐帧差分 | 相对基准 |
|---|---|---|
| 开，模糊半径 10（默认） | 2.48 | 1.0× |
| 开，模糊半径 5 | 4.92 | 2.0× |
| 开，模糊半径 2 | 12.08 | 4.9× |
| **关闭** | **14.62** | **5.9×** |

→ 景深模糊平均了每像素约 100 次采样，把原始噪点压掉约 6 倍。关景深或调小"最大模糊半径"都会让噪点暴涨。
（注意：关闭景深后空间高频 20.06 里也包含了变锐利的真实边缘，但逐帧差分 14.62 是纯噪声指标，不受边缘影响。）

### E5 折射率范围收窄（1.35–1.55，默认 1.2–1.8）
逐帧差分 2.30 ≈ 基准 2.48 → **几乎无效**。收窄折射率只缩小彩虹色边的宽度，不减少采样方差，不是降噪手段。

### E6 性能基准（1280×720，本机实测 rAF 帧率）

| 配置 | 帧率 |
|---|---|
| 最高：色散8 × 弹射10 | 60 fps（满帧） |
| 默认：色散5 × 弹射10 | 60 fps（满帧） |
| 省电档：色散3 × 弹射4 | 60 fps（满帧） |
| 最低：色散1 × 弹射1 | 60 fps（满帧） |

→ 本机 GPU 对这个 shader 有大量余量。**性能与噪点正交**：渲染器没有时间累积机制，帧率下降只会让动画卡顿，不会增加噪点；提高帧率也不会减少噪点。

### E7 原版对照
Shadertoy 官方页面（`analysis/00-*.png`，其 UI 显示 40fps @ 1024×576）的球体暗部有与本地完全相同特征的紫色颗粒 → **噪点是原作自带的，非本地副本引入**。

---

## 五、数据汇总表

| 配置 | 逐帧差分（物体区） | 空间高频 |
|---|---|---|
| 同时刻两帧（确定性对照） | 0.00 | — |
| **默认：色散5/弹射10/景深开/模糊10** | **2.48** | **2.60** |
| 色散 1 | 6.25 | 11.51 |
| 色散 3 | 2.76 | 3.22 |
| 色散 8 | 2.32 | 2.27 |
| 弹射 1 | 0.00 | 0.00 |
| 景深关 | 14.62 | 20.06 |
| 模糊半径 5 | 4.92 | 4.90 |
| 模糊半径 2 | 12.08 | 12.97 |
| 折射率 1.35–1.55 | 2.30 | 2.22 |
| 背景区（任何配置） | 0.00 | 0.00 |

---

## 六、实用建议（按降噪效果排序）

1. **色散采样数调到 8**（面板"色散采样数"）：最直接的降噪旋钮，本机满帧跑动无代价；若帧率吃紧再降回 5。
2. **保持景深开启，"最大模糊半径"不要低于 5**：景深是这个 shader 免费的降噪器，关掉它噪点 ×5.9。
3. **静止观察/截图时按空格暂停**：颗粒不再每秒重掷 60 次，"闪烁感"消失，观感立刻干净一大半——颗粒本身在暂停后只是一层固定的磨砂纹理。
4. 折射率范围收窄**不能降噪**（实测），但能收窄彩虹色边的宽度，纯看喜好。
5. **终极方案（需改代码）**：给运行器加"时间累积降噪"——静止时把最近 N 帧（每帧抖动序列不同）加权平均，噪点随帧数收敛到 0，等效于把 1 spp 提升到 N spp。需要的话可以随时加一个开关。

---

## 七、附录：实验期间的意外发现

连续多小时运行 + 高强度 GPU 实验会触发浏览器丢弃 WebGL 上下文（画面突然变黑）。这不是噪点来源，但已给运行器加上自动恢复：`webglcontextlost` 事件触发后 1.2 秒自动刷新页面重新初始化。

---

*报告生成：2026-09-07 · 测量脚本：浏览器内逐像素差分 · 原始截图见 `analysis/` 目录*
