# 版本馆（VERSIONS）

每个重大变化 = 一个 git 标签版本。命令行查看全部版本：`git tag -n`

## 版本列表

| 版本 | 内容 |
|------|------|
| **v0.1** | 初次复刻：原版碎裂球体 WebGL2 运行器（shader 未参数化，无面板） |
| **v0.2** | 参数面板：22 项滑杆实时调节（加载时注入原作 shader，球体） |
| **v0.3** | 碎裂方块模型：uShape 切换球体/方块，参数直接烘焙进 shader，修复 Image 读上一帧延迟 |
| **v0.4** | 噪点成因分析报告 + WebGL 上下文丢失自动恢复 |
| **v1.0** | 版本管理基建：版本馆启动器（versions.bat）+ 版本索引（当前版本） |

> 说明：v0.1–v0.4 为过程复现——按当时各阶段实际的文件状态重建的提交，内容忠实于开发顺序。

## 如何打开某个版本

1. **双击 `versions.bat`**（推荐）→ 窗口保持打开，输入 编号/标签名 切换版本，**已打开的网页会
   自动刷新成新版本**，可以连续切换对比；`l` 重新列表，`q` 退出（退出后网页仍可继续浏览）
2. **对 ZCode 说**："打开 v1.0 版本" / "列出版本"
3. **手动命令**：`node versions.js v0.2`（供脚本调用）

快照统一挂载在 `../orb-versions/current/`（只读参观用），删除 `orb-versions/` 即可清理，主项目不受影响。

## 版本约定（打新版本三步）

```bash
git tag -a v1.1 -m "一句话说明这个版本做了什么"
git -c http.proxy=http://127.0.0.1:10080 push origin main --tags
# 同时把新版本加进上面的表格
```

版本号习惯：`v1.x` 功能新增 / `v2.0` 大改或破坏性变化 / 临时实验不打标签，用分支。
